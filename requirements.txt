# Core web framework
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.1
python-multipart>=0.0.9

# Validation + config
pydantic>=2.7
pydantic-settings>=2.2

# HTTP (provider calls + Jira)
httpx>=0.27

# End-to-end OpenTelemetry monitoring (local Phoenix collector + UI)
arize-phoenix>=18.0.0
arize-phoenix-otel>=0.16.1
opentelemetry-instrumentation-fastapi>=0.64b0
opentelemetry-instrumentation-httpx>=0.64b0

# Graph execution engine (Dify DSL)
graphon>=0.1.0

# Spreadsheet ingestion
pandas>=2.2
openpyxl>=3.1   # .xlsx read/write
xlrd>=2.0        # legacy .xls read

# Misc
PyYAML>=6.0

# --- Dev / test (optional, see [project.optional-dependencies]) ---
# pytest>=8.2
# pytest-asyncio>=0.23
# respx>=0.21
